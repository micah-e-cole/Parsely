### Packaging & Deployment

- [ ] Build a self-contained Parsely executable using **PyInstaller**
    - Should include all required libraries (ttkbootstrap, Pillow, PyPDF2, openpyxl, python-docx)
    - Must run **without admin rights** or external dependencies
    - Goal: single `Parsely.exe` for Windows users on managed devices
    - Add cross-platform builds (Windows `.exe`, macOS `.app`)

← [Back to README](README.md)
